<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_estudos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_estudos_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Controledeestudos">

        <activity
            android:name=".DisciplinaActivity"
            android:exported="false"
            android:parentActivityName=".ListaConteudosActivity" />

        <activity
            android:name=".ConfiguracoesActivity"
            android:exported="false" />

        <activity
            android:name=".SobreActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".ListaConteudosActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>